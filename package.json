{
  "name": "nuxt-express-ts",
  "version": "1.0.0",
  "description": "Nuxt+Express with TypeScript",
  "author": "negibouze",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.ts --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.ts",
    "generate": "nuxt generate",
    "test": "ts-node server/index.ts"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "nuxt": "^2.4.0",
    "ts-node": "^8.0.3",
    "vue-property-decorator": "^8.1.0"
  },
  "devDependencies": {
    "@nuxt/typescript": "^2.6.1",
    "@types/express": "^4.16.1",
    "nodemon": "^1.18.9"
  }
}
